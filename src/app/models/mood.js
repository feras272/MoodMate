export default class Mood {
    constructor(key, day, month, mood, phrases, emoji) {
      this.key = key;
      this.day = day;
      this.month = month;
      this.mood = mood;
      this.phrases = phrases;
      this.emoji = emoji;
    }
  }